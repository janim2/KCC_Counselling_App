import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CounsellorDetailsScreen displays counsellor details correctly',
      (WidgetTester tester) async {
    // Mock the rootBundle.loadString method.
    //   const mockCounsellorDetails = 'Mock counsellor details text.';
    //   const mockAssetPath = 'assets/files/bernice.txt';
    //   final mockLoadString = (String assetPath) async {
    //     if (assetPath == mockAssetPath) {
    //       return mockCounsellorDetails;
    //     }
    //     throw Exception('Failed to load asset');
    //   };

    //   // Set up the mock rootBundle.loadString.
    //   TestWidgetsFlutterBinding.ensureInitialized();
    //   // rootBundle.loadString = mockLoadString;

    //   // Build the widget and trigger a frame.
    //   await tester.pumpWidget(InitialScreen());
    //   // Navigate to the CounsellorDetailsScreen.
    //   await tester.tap(find.text('Navigate to CounsellorDetailsScreen'));
    //   // Wait for animations to complete.
    //   await tester.pumpAndSettle();

    //   // Verify that the BackButton widget is displayed.
    //   expect(find.byType(BackButton), findsOneWidget);

    //   // Verify that the CounsellorInfoCard widget is displayed.
    //   expect(find.byType(CounsellorInfoCard), findsOneWidget);

    //   // Verify that the CircularStructureWithWidgetsInside widget is displayed.
    //   expect(find.byType(CircularStructureWithWidgetsInside),findsOneWidget);

    //   // Verify that the counsellor details text is displayed.
    //   expect(find.text(mockCounsellorDetails), findsOneWidget);
  });
}
